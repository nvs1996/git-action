name: Build and Deploy
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: mvn -v
      - run: java -version
      - run: cd ${{ github.workspace }}
      - run: ls -l
      - run: mvn package
      - run: mvn deploy
      - name: Build and Deploy
        uses: actions/build-deploy@v2
